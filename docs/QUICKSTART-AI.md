# Quickstart: AI Навички та Робочі Процеси

## 1. Налаштування

Запустіть скрипт налаштування, щоб ініціалізувати середовище та клонувати необхідні репозиторії:

**Windows (PowerShell):**
```powershell
.\tools\setup-ai.ps1
```

**Linux/Mac:**
```bash
./tools/setup-ai.sh
```

## 2. Використання Маршрутизатора завдань (Task Router)

Маршрутизатор завдань допомагає вибрати правильний стек та інструменти для вашого завдання.

**Команда:**
`/start-task` (або вручну через запит)

**Робочий процес:**
1.  Опишіть ваше завдання.
2.  Маршрутизатор аналізує ваш запит і пропонує "стек" (наприклад, React + Vite + Tailwind).
3.  Після підтвердження він активує відповідний **Профіль** (наприклад, UI Профіль).

## 3. Профілі

Ви також можете вручну запитати профіль:

*   "Активуй профіль DevOps" -> Завантажує навички Ops.
*   "Активуй профіль UI" -> Завантажує навички Дизайн-системи та Фронтенду.

## 4. Рецепти

### 🛡️ Реагування на інциденти (DevOps)
**Сценарій**: Production API видає помилки 500.
1.  **Активація**: `powershell .\tools\activate-profile.ps1 DevOps`
2.  **Запит**: "Використовуй `incident-response`, щоб проаналізувати останні 100 рядків логів у `./logs/error.log` і запропонувати виправлення."

### 🎨 Редизайн інтерфейсу (Фронтенд)
**Сценарій**: Оновити дизайн панелі керування за допомогою нової Дизайн-системи.
1.  **Активація**: `powershell .\tools\activate-profile.ps1 UI`
2.  **Запит**: "Прочитай `design-system/MASTER.md`. Використовуй `ui-ux-pro-max`, щоб згенерувати React-компонент для UserCard, що відповідає нашій типографіці."

### 📝 Оновлення документації (Документація)
**Сценарій**: Задокументувати нову кінцеву точку API.
1.  **Активація**: `powershell .\tools\activate-profile.ps1 Docs`
2.  **Запит**: "Використовуй `technical-writing`, щоб підготувати чернетку запису в README для `POST /api/v1/users`."

### 🧠 Планування складних функцій (Загальне)
**Сценарій**: Побудова мультитенантної системи автентифікації.
1.  **Переконайтеся**: Superpowers (Plan/Execute) активні (за замовчуванням).
2.  **Запит**: "Створи IMPLEMENTATION_PLAN.md для Auth за допомогою `writing-plans`. Розбий його на 3 фази."

## 5. Що можна і чого не можна робити

*   **МОЖНА** використовувати Маршрутизатор для нових функцій, щоб забезпечити послідовність.
*   **МОЖНА** перевіряти `SETUP-LOG.md`, щоб побачити, які зміни були внесені агентом.
*   **НЕ МОЖНА** вручну редагувати файли в `.claude/vendor`. Замість цього використовуйте скрипти курації.
